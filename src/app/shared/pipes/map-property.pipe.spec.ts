import { MapPropertyPipe } from './map-property.pipe';

describe('MapPropertyPipe', () => {
  it('create an instance', () => {
    const pipe = new MapPropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
